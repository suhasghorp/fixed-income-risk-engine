# Adds to the demo profile (--spring.profiles.active=demo,demo-slow): ticks every 200ms while each repricing
# cycle is slowed to 1s, so the ticks a cycle misses are visibly coalesced while the simulation keeps its pace.
risk.simulation.tick-interval=200ms
risk.repricing.cycle-delay=1s
